{
  "name": "jest-memory-leak",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node --expose-gc ./node_modules/.bin/jest --logHeapUsage --detectLeaks --runInBand"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jest": "^25.0.0",
    "weak-napi": "^1.0.3"
  }
}
